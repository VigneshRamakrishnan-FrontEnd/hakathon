import React from "react";
import ReactDOM from "react-dom/client";
import Boot from "./questions/boot";
import Quest5 from "./questions/quest5";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <Quest5 />
  </React.StrictMode>
);
